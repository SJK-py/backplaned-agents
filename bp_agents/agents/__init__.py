"""bp_agents.agents — the suite's agent processes.

Each subpackage is one external SDK agent (one process per agent in v1).
`python -m bp_agents.agents.<name>` runs it. See [agent-suite/agents.md].
"""
